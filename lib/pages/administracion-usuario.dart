import 'package:flutter/material.dart';

class AdministracionUsuario extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Administracion usuario'),
      ),
      body: Container(
        child: Center(
          child: Text('Esto es un texto !'),
          )
      ),
    );
  }
}
