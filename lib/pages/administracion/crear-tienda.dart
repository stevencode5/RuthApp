import 'package:flutter/material.dart';

class CrearTienda extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Crear tienda'),
      ),
      body: Container(
          child: Center(
        child: Text('Creando tienda ....'),
      )),
    );
  }

}
