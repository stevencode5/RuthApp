import 'package:flutter/material.dart';

class SuscribirseTienda extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Suscribirse a tienda'),
      ),
      body: Container(
          child: Center(
        child: Text('Listado de tiendas ....'),
      )),
    );
  }

}
